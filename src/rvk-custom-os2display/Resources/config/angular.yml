assets:
  js_prod:
    - bundles/rvkcustomos2display/dist/bbs-tools.min.js

modules:
  bbsTools:
    dependencies:
      - slidesInSlide
      - toolsModule
    # Please don't add more files here. Add it in the gulpfile so it gets
    # compiled into the bbs-tools.min.js. The reason is that Os2Display
    # include different files on prod and dev and we are trying to minimize
    # errors by keeping it consistent.
    files:
      - bundles/rvkcustomos2display/dist/bbs-tools.min.js
